<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <template id="meta_ads_dashboard_template" name="Meta Ads Dashboard">
    <t t-call="web.layout">
      <div class="o_meta_ads_dashboard">
        <!-- Header / Filtros -->
        <div class="dashboard-header">
          <div class="brand">
            <img src="/odoo_meta_reporting/static/description/icon.png" alt="Meta" class="brand-logo"/>
            <h1>Dashboard Meta Ads</h1>
          </div>
          <div class="filters">
            <select id="filter-account" class="filter-control" title="Cuenta publicitaria de Meta Ads">
              <option value="">Seleccionar cuenta...</option>
              <t t-foreach="accounts" t-as="account">
                <option t-att-value="account.id" t-esc="account.name"/>
              </t>
            </select>
            <input id="filter-campaign" type="text" class="filter-control" placeholder="Campaña (opcional)" title="Filtrar por nombre exacto de campaña"/>
            <input id="filter-date-from" type="date" class="filter-control" t-att-value="default_date_from" title="Fecha inicial"/>
            <input id="filter-date-to" type="date" class="filter-control" t-att-value="default_date_to" title="Fecha final"/>
            <button id="btn-refresh" class="btn-refresh" title="Consultar datos en BigQuery">Actualizar</button>
          </div>
        </div>

        <!-- Loading -->
        <div id="dashboard-loading" class="dashboard-loading hidden">Cargando datos...</div>
        <div id="dashboard-error" class="dashboard-error hidden"></div>

        <!-- KPIs -->
        <div class="kpi-grid" id="kpi-grid">
          <div class="kpi-card" title="Gasto total en publicidad durante el período seleccionado.">
            <div class="kpi-label">Gasto total <span class="help-icon">?</span></div>
            <div class="kpi-value" id="kpi-spend">-</div>
          </div>
          <div class="kpi-card" title="Número de veces que se mostraron tus anuncios.">
            <div class="kpi-label">Impresiones <span class="help-icon">?</span></div>
            <div class="kpi-value" id="kpi-impressions">-</div>
          </div>
          <div class="kpi-card" title="Clics que hicieron en el enlace de tu anuncio.">
            <div class="kpi-label">Clics en enlace <span class="help-icon">?</span></div>
            <div class="kpi-value" id="kpi-clicks">-</div>
          </div>
          <div class="kpi-card" title="Click-Through Rate: porcentaje de impresiones que terminaron en clic.">
            <div class="kpi-label">CTR <span class="help-icon">?</span></div>
            <div class="kpi-value" id="kpi-ctr">-</div>
          </div>
          <div class="kpi-card" title="Costo por clic: cuánto pagás en promedio por cada clic.">
            <div class="kpi-label">CPC <span class="help-icon">?</span></div>
            <div class="kpi-value" id="kpi-cpc">-</div>
          </div>
          <div class="kpi-card" title="Número de personas únicas que vieron tus anuncios.">
            <div class="kpi-label">Alcance <span class="help-icon">?</span></div>
            <div class="kpi-value" id="kpi-reach">-</div>
          </div>
          <div class="kpi-card" title="Promedio de veces que cada persona vio tus anuncios.">
            <div class="kpi-label">Frecuencia <span class="help-icon">?</span></div>
            <div class="kpi-value" id="kpi-frequency">-</div>
          </div>
        </div>

        <!-- Gráficos principales -->
        <div class="chart-grid">
          <div class="card chart-card" title="Evolución diaria de las métricas principales.">
            <h3>Rendimiento diario <span class="help-icon">?</span></h3>
            <canvas id="chart-daily"></canvas>
          </div>
          <div class="card chart-card" title="Distribución por plataforma (Facebook, Instagram, etc.).">
            <h3>Ubicación <span class="help-icon">?</span></h3>
            <canvas id="chart-placement"></canvas>
          </div>
        </div>

        <!-- Embudo -->
        <div class="card funnel-card" title="Embudo de conversión: de impresiones a clics.">
          <h3>Embudo <span class="help-icon">?</span></h3>
          <div class="funnel" id="funnel">
            <div class="funnel-step">
              <span class="funnel-label">Impresiones</span>
              <span class="funnel-value" id="funnel-impressions">-</span>
            </div>
            <div class="funnel-arrow">↓</div>
            <div class="funnel-step">
              <span class="funnel-label">Clics en enlace</span>
              <span class="funnel-value" id="funnel-clicks">-</span>
              <span class="funnel-rate" id="funnel-ctr">-</span>
            </div>
          </div>
        </div>

        <!-- Tablas -->
        <div class="card" title="Resultados agrupados por campaña.">
          <h3>Campañas <span class="help-icon">?</span></h3>
          <table class="data-table" id="table-campaigns">
            <thead>
              <tr>
                <th>Nombre de campaña</th>
                <th class="numeric">Gasto</th>
                <th class="numeric">Impresiones</th>
                <th class="numeric">Clics</th>
                <th class="numeric">CTR</th>
              </tr>
            </thead>
            <tbody></tbody>
          </table>
        </div>

        <div class="card" title="Resultados agrupados por anuncio o creatividad.">
          <h3>Anuncios / Creatividades <span class="help-icon">?</span></h3>
          <table class="data-table" id="table-ads">
            <thead>
              <tr>
                <th>Campaña</th>
                <th>Nombre del anuncio</th>
                <th class="numeric">Gasto</th>
                <th class="numeric">Impresiones</th>
                <th class="numeric">Clics</th>
                <th class="numeric">CTR</th>
              </tr>
            </thead>
            <tbody></tbody>
          </table>
        </div>
      </div>

      <link rel="stylesheet" href="/odoo_meta_reporting/static/src/css/dashboard.css"/>
      <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/chart.js@2.9.4/dist/Chart.min.js"/>
      <script type="text/javascript" src="/odoo_meta_reporting/static/src/js/dashboard.js"/>
    </t>
  </template>

</odoo>
